<p>Last Execution Time: <?= $block->escapeHtml($block->getLastExecutionTime()) ?></p>
<table class="data table" id="custom-config-table">
    <thead>
        <tr>
            <th>Parent Key</th>
            <th>Key</th>
            <th>Value</th>
        </tr>
    </thead>
    <tbody>
        <?php foreach ($block->flattenConfigArray($block->getConfigArray()) as $data): ?>
            <tr>
                <td><?= $block->escapeHtml($data['parent_key']) ?></td>
                <td><?= $block->escapeHtml($data['key']) ?></td>
                <td><?= $block->escapeHtml($data['value']) ?></td>
            </tr>
        <?php endforeach; ?>
    </tbody>
</table>
